import { Hero as H, Container, HeroTitle } from './styles'

const Hero = () => (
  <H>
    <Container>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Container>
  </H>
)

export default Hero
